# Scenario 02 — Open Workspace

Read-only `list_files` on the workspace root.

```bash
python -m hermes_openclaw --plan examples/02_open_workspace/plan.json --dry-run
```

Expected: validation passes at Safe level; no confirmation required.
