# Scenario 01 — Organize Files

Creates folders and lists files in the workspace. Typical write + read plan.

```bash
python -m hermes_openclaw --plan examples/01_organize_files/plan.json --dry-run
```

Expected: validation passes; dry-run reports the planned actions.
